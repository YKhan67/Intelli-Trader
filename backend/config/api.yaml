api:
  host: "0.0.0.0"
  port: 8000
  cors_origins:
    - "http://localhost"
    - "http://localhost:8000"
    - "*"
  rate_limit:
    requests_per_minute: 60
  auth:
    api_key_env_var: "FOREXAI_API_KEY"
  websockets:
    ping_interval: 30
    ping_timeout: 10
